/**
 * Represents the role assigned to legend gradient elements in a visualization context.
 *
 * This constant is used to identify and style legend gradient components within the visualization.
 * Legend gradients are typically used to represent continuous data scales with color gradients.
 *
 * @constant {string}
 * @default 'legend-gradient'
 * 
 * @example
 * // Example usage in a visualization component:
 * const gradientRole = LegendGradientRole;
 * applyRoleToElement(gradientElement, gradientRole);
 */
const LegendGradientRole = 'legend-gradient';
